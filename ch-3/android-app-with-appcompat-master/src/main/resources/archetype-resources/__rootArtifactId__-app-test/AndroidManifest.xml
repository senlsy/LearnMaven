#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:versionCode="1" android:versionName="${version}" package="${package}_test">

    <uses-sdk android:minSdkVersion="8" android:targetSdkVersion="${android-api-level}"/>

    <instrumentation android:name="android.test.InstrumentationTestRunner" android:targetPackage="${package}"/>

    <application android:allowBackup="true">
        <uses-library android:name="android.test.runner"/>
    </application>

</manifest>
